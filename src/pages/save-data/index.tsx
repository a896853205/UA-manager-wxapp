import Taro, { memo } from '@tarojs/taro';
import { View } from '@tarojs/components';

const SaveData = () => {
  return <View>{/* TODO: 表单,数据input(1-3),时间选择,保存按钮 */}</View>;
};

export default memo(SaveData);
